# Mobicom

Canonical Mobicom website source.

## Hosting

This repository is prepared for Firebase Hosting. The deploy directory is `public`.

## Local preview

Serve the `public` directory with any static web server.

## Deployment

Connect this repository to the Mobicom Firebase project, then deploy with `firebase deploy --only hosting`.
